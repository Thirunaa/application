# This webapplication is built as a simple job application portal for a company.

# Front end is built using ReactJS and backend is built using NodeJS.

# The application is deployed on AWS EC2 instance.
